/**
 * Socket.IO gateway: qr, connected, disconnected, message.
 * Implement when @nestjs/websockets + socket.io are added.
 * Spec: WHATSAPP-QR-ENTERPRISE-SPEC.md §6
 */
import { Injectable } from '@nestjs/common';

@Injectable()
export class WhatsAppQrGatewayPlaceholder {}
