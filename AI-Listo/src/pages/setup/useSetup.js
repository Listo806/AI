import {useCallback,useEffect,useRef,useState} from 'react';import {setupApi} from './setupApi';
export function useSetup(){const [data,setData]=useState(null),[state,setState]=useState('');const timer=useRef();const load=useCallback(async()=>{setData(await setupApi.get())},[]);useEffect(()=>{load().catch(e=>setState(e.message))},[load]);const save=useCallback((patch,immediate=false)=>{setData(d=>d?({...d,selected_objective:patch.selectedObjective??d.selected_objective,config:{...d.config,...patch}}):d);clearTimeout(timer.current);const go=async()=>{try{setState('Saving…');setData(await setupApi.save(patch));setState('All changes saved')}catch(e){setState('Unable to save — Try again')}}; immediate?go():timer.current=setTimeout(go,500)},[]);return{data,state,save,load}}
